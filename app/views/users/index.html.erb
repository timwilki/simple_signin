<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if current_user.superadmin? && !current_user?(@user) %>

<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>

<% else %>

<ul class="users">
  <%= render @org_users %>
</ul>

<% end %>
